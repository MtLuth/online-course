import BecomeInstructor from "@/sections/(landing)/intro/BecomeInstructor";

export const metadata = {
  title: "Giới thiệu đăng ký trở thành chuyên gia | Elearning",
};

export default function RegisterInstructor() {
  return <BecomeInstructor />;
}
